import styled from "styled-components"
import {StatusBar} from "react-native"
export const MainConatiner= styled.SafeAreaView`
flex:1;
paddingTop:${StatusBar.currentHeight}px;
background-color:#f0f0f0;
`